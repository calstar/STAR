'use client'

/**
 * WindowTopBar is no longer needed — TopBar now renders in ALL contexts
 * (main page + popup windows) with pressure bars and abort buttons.
 * This file is kept as a no-op so imports don't break.
 */
export default function WindowTopBar() {
  return null;
}
