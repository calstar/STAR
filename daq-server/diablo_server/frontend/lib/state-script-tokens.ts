// Re-export of the shared tokenizer, so frontend code imports through @/lib like the rest of the
// config tooling. The implementation lives in diablo_server/shared.
export * from '../../shared/state-script-tokens.js';
