[Unit]
Description=Sensor System Actuator Service (C++ — state→UDP commands)
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/sensor_system
ExecStart=/bin/bash -c 'if [ -x ./build/bin/sequencer_service ]; then exec ./build/bin/sequencer_service --config config/config.toml --port 9998; else echo "sequencer_service not found — run: cd build && cmake .. && make sequencer_service"; exit 1; fi'
Restart=always
RestartSec=5
Environment=PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

[Install]
WantedBy=default.target
