#pragma once

/**
 * Shared ADC configuration constants.
 * This file defines constants used by both the GUI and the C++ firmware.
 * Keep this file in sync with the Python config adc_config.py
 */

// Number of readings per mux channel per sample
#define READINGS_PER_MUX 10




