# Root .env — shared secrets consumed by docker-compose.
# Copy to .env and fill in values. Never commit .env to git.

# Must match the value in auth/.env
JWT_SECRET=replace-with-a-long-random-string
