# Doxygen output
docs/doxygen/

# Claude Code
.claude/

# PlatformIO build artifacts
.pio/
**/.pio/

# DAQ output data
scripts/postprocessing/output/

# Secrets — never commit these
.env
*.env
auth/.env
# onshape-viewer approved-users roster — lives on the server only, mounted into
# the auth container. Template is onshape_allowlist.example.txt.
onshape_allowlist.txt

# Python / Virtualenvs
.venv/
**/.venv/
venv/
__pycache__/
**/__pycache__/
*.py[cod]

# Node / dependencies
node_modules/
**/node_modules/
dist/
**/dist/
.next/
**/.next/

# Internal review notes (not shared with the team)
daq-server/BACKEND_REVIEW.md
