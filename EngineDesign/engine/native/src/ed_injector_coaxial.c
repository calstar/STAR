/* ed_injector_coaxial.c - STAGE: deferred port. See engine/native/README.md "Staged plan".
 * Placeholder translation unit reserved so the source layout and build graph
 * match the target architecture. The corresponding Python physics has been read
 * and mapped; implementation lands in a follow-up stage with golden parity tests.
 */
#include "ed_types.h"

/* Internal version tag keeps this a non-empty, ISO-C-valid translation unit. */
const char *ed_injector_coaxial_stage(void) { return "deferred"; }
