[connection]
# Ubuntu ships this file with wifi.powersave = 3 (enabled). On USB Realtek
# adapters (rtw88_*) a power-save desync wedges the association: the link stays
# "connected" and passes zero frames. 2 = disable power save.
wifi.powersave = 2
