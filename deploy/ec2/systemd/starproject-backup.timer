[Unit]
Description=Daily STARProject → S3 backup

[Timer]
# 03:30 UTC daily (offset from the legacy OpenProject timer at 03:00).
# Persistent=true runs a missed backup (box was off) on boot.
OnCalendar=*-*-* 03:30:00
Persistent=true
RandomizedDelaySec=300

[Install]
WantedBy=timers.target
