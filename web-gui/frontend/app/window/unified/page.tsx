'use client'

import UnifiedDashboard from '@/components/dashboard/UnifiedDashboard';

// ── Single Pane Unified Dashboard Window ────────────────────────────────────
export default function UnifiedDashboardPage() {
  return <UnifiedDashboard />;
}
