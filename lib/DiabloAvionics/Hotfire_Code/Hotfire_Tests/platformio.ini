; Hotfire Unit Tests — runs on host PC (no ESP32 needed)
;
; Run tests:  pio test -e native
;             (from this directory)

[env:native]
platform = windows_x86
build_flags =
    -std=gnu++11
    -I stubs
    -I ../../libraries/DAQv2-Comms/src
    -I ../common
test_framework = unity
