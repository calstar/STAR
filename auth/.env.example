# ── Google OAuth credentials ───────────────────────────────────────────────
# Create at https://console.cloud.google.com → APIs & Services → Credentials
# Add authorized redirect URIs:
#   https://auth.starberkeley.org/callback   (production)
#   http://localhost:5000/callback            (local testing)

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ── JWT / cookie ───────────────────────────────────────────────────────────
# Must be the same value across auth service, EngineDesign, pid-designer, and DAQ server.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"

JWT_SECRET=replace-with-a-long-random-string

# ── Flask internal session secret ─────────────────────────────────────────
# Only needed by the auth service itself for OAuth state handling.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"

FLASK_SECRET_KEY=replace-with-another-long-random-string

# ── Optional overrides ─────────────────────────────────────────────────────
# COOKIE_DOMAIN=.starberkeley.org
#   Domain the session cookie is scoped to. Everything from it is derived:
#   which hosts count as "ours" for redirects, and which are treated as
#   production. Compose passes this through as BASE_DOMAIN.
#
# AUTH_PUBLIC_URL=https://auth.starberkeley.org
#   Where this service is reachable from a browser. Used to build the login
#   link that /verify sends unauthenticated visitors to, so it must be the
#   public URL, not the container address.
#
# ALLOWED_EMAIL_DOMAIN=berkeley.edu
# TOKEN_TTL_DAYS=30
