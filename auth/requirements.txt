# Pinned deliberately. Left unpinned, pip has to resolve authlib against
# whatever cryptography wheel it can reach, and a single hiccup sends it
# backtracking through a dozen authlib releases until it gives up with
# ResolutionImpossible -- an image that builds or not depending on the day.
#
# To upgrade: bump here, rebuild, run `python -m pytest test_main.py`.
flask==3.1.3
authlib==1.7.2
requests==2.34.2
pyjwt==2.13.0
python-dotenv==1.2.2
markupsafe==3.0.3
gunicorn==26.0.0
