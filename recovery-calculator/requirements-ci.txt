# CI-only requirements: everything in requirements.txt plus the test runner.
#
# Split out to match EngineDesign, where CI installs a superset that the dev
# environment does not need to carry.

-r requirements.txt

pytest>=7.0
pytest-timeout>=2.1

# fastapi's TestClient is a thin wrapper over an HTTP client and needs one.
# starlette wants httpx2; plain httpx still works but deprecation-warns.
httpx2>=0.28
