/**
 * Button styles and the date formatter, now shared with the other design tools.
 *
 * Kept as a re-export rather than changing every import site.
 */

export { btn, primaryBtn, dangerBtn, ghostBtn, relativeTime } from '@stardesign-ui';
