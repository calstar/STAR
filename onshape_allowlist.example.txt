# onshape-viewer approved users — TEMPLATE.
#
# Copy to `onshape_allowlist.txt` (gitignored) ON THE SERVER and list the
# approved emails there. The real file is never committed — the roster lives only
# on the machine. docker-compose.yml bind-mounts it into the auth container at
# /app/onshape_allowlist.txt, and it is re-read on change (no restart needed).
#
# Format: one email per line; `#` starts a comment; blank lines ignored;
# matching is case-insensitive.
#
# A valid @berkeley.edu session is authenticated everywhere, but only the emails
# listed here may use onshape-viewer (it brokers Onshape API credentials). If the
# file is missing, onshape-viewer denies everyone (fail-closed).
#
# Fill in real emails on the server, e.g.:
# aidan@berkeley.edu
# teammate@berkeley.edu
